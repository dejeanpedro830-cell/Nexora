# NEXORA

MAKE PROGRESS REAL.

This is the phone-friendly static NEXORA build for testing and GitHub Pages.

The AI Coach uses the built-in local fallback in this static version. The full server-side AI version can be deployed later on a backend platform without exposing an API key in the browser.
